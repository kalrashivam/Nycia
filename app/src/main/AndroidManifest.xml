<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.designcut.nycia">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PortalActivity">
        </activity>
        <activity android:name=".SignUpActivity" />
        <activity android:name=".Salon_SignupActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/MainActivuty" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".user.Salon_Details" />
        <activity
            android:name=".Salon.Owner_MainActivity"
            android:label="@string/title_activity_owner__main"
            android:theme="@style/MainActivuty">

        </activity>
        <activity
            android:name=".Salon.OwnerADDIMage_Activity"
            android:theme="@style/MainActivuty" />

        <activity
            android:name=".Salon.Max_BookingsActivity"
            android:theme="@style/MainActivuty" />

        <activity android:name=".Salon.Update_DetailsActivity"
            android:theme="@style/MainActivuty"/>
    </application>

</manifest>